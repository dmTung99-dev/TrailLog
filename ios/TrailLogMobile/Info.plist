<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Placeholder Info.plist. The real ios/ Xcode project (generated by
  `npx react-native init`) was not scaffolded in this environment — see
  Task 1's report. This file holds only the keys this plan's tasks add, so
  they exist somewhere and can be merged into a real generated Info.plist
  later. It is NOT a buildable native project on its own.
-->
<plist version="1.0">
<dict>
    <key>UIBackgroundModes</key>
    <array>
      <string>location</string>
    </array>
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>TrailLog needs your location to record your route.</string>
    <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
    <string>TrailLog needs your location to keep recording your route while your screen is off.</string>
</dict>
</plist>
