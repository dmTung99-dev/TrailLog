<?xml version="1.0" encoding="utf-8"?>
<!--
  Placeholder AndroidManifest.xml. The real android/ project (generated by
  `npx react-native init`) was not scaffolded in this environment — see
  Task 1's report. This file holds only the permissions/service entries
  this plan's tasks add, so they exist somewhere and can be merged into a
  real generated manifest later. It is NOT a buildable native project on
  its own.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <application>
        <service android:name="com.asterinet.react.bgactions.RNBackgroundActionsTask" android:foregroundServiceType="location" />
    </application>
</manifest>
